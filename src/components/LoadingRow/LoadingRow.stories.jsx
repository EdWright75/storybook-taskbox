import React from 'react';

import LoadingRow from './LoadingRow';

export default {
    component: LoadingRow,
    title: 'LoadingRow',
};

const Template = () => <LoadingRow />

export const Default = Template.bind({});